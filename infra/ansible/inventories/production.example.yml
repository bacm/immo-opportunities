---
immo:
  hosts:
    immo-production:
      ansible_host: 203.0.113.20
      ansible_user: deploy
      ansible_port: 22
  vars:
    immo_environment: production
    immo_domain: immo.example.com
    immo_admin_cidrs: ["0.0.0.0/0", "::/0"]
    immo_acme_email: ops@example.com
